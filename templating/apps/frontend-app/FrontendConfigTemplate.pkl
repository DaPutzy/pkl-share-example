amends ".../TenantConfig.pkl"

configs = new Listing {
  for (tenant in tenants) {
    when (tenant.frontend != null) {
      new {
        domainName = tenant.frontend.domain
      }
    }
  }
}

output {
  value = configs
  renderer = new JsonRenderer {
    extension = "json5"
  }
}
